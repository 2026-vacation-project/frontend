# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 같이 게임할 사람을 빠르게 찾고 싶은 한국의 개인 사용자.
- 친구 또는 기존 팀원과 그룹을 만들고 역할과 반복 플레이를 관리하는 그룹 구성원과 관리자.

## Product Purpose

팀모아는 같이 게임할 사람을 찾는 과정의 번거로움을 줄이고, 원하는 게임·인원·포지션 조건에 맞는 사람들과 빠르게 파티를 구성하게 한다. 사용자는 모집을 탐색하거나 만들고 참가하며, 그룹 안에서 멤버와 역할을 지속적으로 관리한다. 성공은 사용자가 현재 모집 상태를 즉시 파악하고 최소한의 단계로 참가하거나 모집을 시작하는 것이다.

## Positioning

단발성 게임 파티 모집과 지속적인 친구 그룹 관리를 하나의 흐름으로 연결한다. 그룹별 역할 체계, 관심 게임, 모집 상태, 알림을 함께 다루어 다음 파티 구성을 더 빠르게 만든다.

## Operating Context

- 비로그인 사용자는 명확히 예시로 표시된 서비스 화면을 살펴본 뒤 Google 또는 Discord 로그인 흐름으로 진입한다. 전역 모집 조회 API가 추가되기 전에는 이를 실제 모집 데이터처럼 표시하지 않는다.
- 로그인 사용자는 홈에서 관심 게임, 현재 모집, 마감 임박 모집, 참여 중인 모집, 그룹 모집을 탐색한다.
- 사용자는 모집방의 게임, 목표 인원, 필요한 포지션을 확인하고 참가하거나 직접 방을 만든다.
- 그룹 관리자는 멤버를 초대하고 역할을 만들고 색상을 정하며 멤버에게 역할을 부여한다.
- 사용자는 관심 게임과 알림 종류를 설정한다. 웹 푸시 권한은 기능을 활성화하는 순간에만 요청한다.

## Capabilities and Constraints

- React, TypeScript, Vite, React Router, 일반 CSS를 사용한다. Tailwind CSS는 사용하지 않는다.
- 전역 디자인 토큰은 CSS Custom Properties로 관리하고 UI와 도메인 컴포넌트를 재사용한다.
- 데스크톱, 태블릿, 모바일 반응형과 키보드 사용을 지원한다.
- API 호출은 서비스 계층에 모으고 `http://127.0.0.1:8000/openapi.json`의 실제 요청·응답 스키마를 그대로 따른다.
- Swagger에 없는 API는 추측하거나 목 엔드포인트로 만들지 않는다. 전역 모집 검색·필터, 그룹 수정·초대, 역할 해제, 알림 목록은 현재 백엔드 미지원 기능으로 명시한다.
- 인증은 Google/Discord Authorization Code 흐름을 사용한다. 프론트엔드는 OAuth 콜백의 `code`를 `POST /api/v1/auth/login/{provider}`로 전달하고, 응답의 JWT를 이후 API 요청에 Bearer Token으로 사용한다.
- 현재 모집방은 그룹 하위에서만 조회할 수 있고 `game_name`, `target_count`, `target_role`, `unit_type` 필드를 사용한다.
- 실제 OAuth 클라이언트 정보, Firebase 프로젝트 설정, 운영 환경 주소는 배포 환경별 환경 변수로 제공한다.

## Brand Commitments

- 제품명은 `팀모아`다.
- `public/favicon.svg`를 임시 로고로 사용한다.
- 친근하고 신뢰감 있으며 활동적인 현대 한국 게임 커뮤니티 서비스여야 한다.
- 게임 전용 제품임을 분명히 하되, 과도한 eSports·스트리밍·기업용 SaaS 인상은 피한다.
- 보라·파랑 SaaS 그라데이션, 무의미한 글래스 효과, 과도한 카드·배지·둥근 모서리·그림자·애니메이션을 사용하지 않는다.
- 한국어 가독성과 명확한 정보 구조를 시각적 화려함보다 우선한다.

## Evidence on Hand

- 상세 제품·UX·기술 요구사항: `PROMPT.md`
- 기존 API와 데이터 모델: `../backend`
- 임시 브랜드 자산: `public/favicon.svg`
- 실제 사용자 수, 후기, 성과 수치, 운영 데이터는 없으며 제품 화면에서 이를 만들어내지 않는다.

## Product Principles

- 빠른 탐색에서 상태 파악, 역할 확인, 참가까지의 흐름을 짧게 유지한다.
- 목록에서는 결정을 돕는 핵심 정보만 보여주고 세부 정보는 상세 화면에서 제공한다.
- 그룹과 역할은 반복적인 팀 구성 시간을 줄이는 도구여야 한다.
- 빈 상태, 로딩, 오류, 권한 거부 상황에서도 다음 행동을 명확히 제시한다.
- 실제 API로 교체하기 쉬운 구조와 일관된 디자인 시스템을 유지한다.

## Accessibility & Inclusion

키보드 탐색, 분명한 포커스, 충분한 대비, 의미 있는 접근성 이름, 입력 레이블, 색상 외 상태 표현, 44px 이상의 주요 터치 영역을 기본 품질로 유지한다.
